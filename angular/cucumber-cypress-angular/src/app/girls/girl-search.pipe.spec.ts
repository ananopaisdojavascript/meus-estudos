import { GirlSearchPipe } from './girl-search.pipe';

describe('GirlSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new GirlSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
