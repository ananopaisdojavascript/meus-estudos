import { GirlSelectPipe } from './girl-select.pipe';

describe('GirlSelectPipe', () => {
  it('create an instance', () => {
    const pipe = new GirlSelectPipe();
    expect(pipe).toBeTruthy();
  });
});
